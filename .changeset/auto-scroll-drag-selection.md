---
'@savvycal/calendar': patch
---

Auto-scroll the grid when dragging a time-range selection near the top or bottom edge, so the growing range stays in view instead of clamping to the visible hours.
